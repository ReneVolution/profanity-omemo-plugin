language: python
python:
  - "2.6"
  - "2.7"

# command to install dependencies
install:
  - python setup.py -q install
  - pip install pytest
  - pip install coveralls

# command to run tests
script: coverage run --source=profanity_omemo_plugin py.test -v --color=yes

after_success:
  coveralls